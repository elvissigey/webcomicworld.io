import { useEffect, useMemo, useState } from 'react'
import type { Session } from '@supabase/supabase-js'
import { supabase } from './lib/supabase'

type View='home'|'browse'|'detail'|'reader'|'library'|'profile'|'studio'|'admin'
type Comic={id:string;title:string;slug:string;description:string;cover_path:string|null;average_rating:number;rating_count:number;followers:number;views:number;status:string;content_status:string;is_featured:boolean;created_by:string;chapters_count:number;last_episode_at:string|null}
type Episode={id:string;comic_id:string;episode_number:number;title:string;description:string;content_status:string;published_at:string|null;views:number;created_by:string}
type Page={id:string;episode_id:string;page_number:number;storage_path:string;alt_text:string}
type Profile={id:string;username:string;display_name:string;avatar_url:string|null;bio:string|null;role:string;creator_approved:boolean;is_banned:boolean;banned_until:string|null}
type Comment={id:string;user_id:string;comic_id:string;episode_id:string|null;parent_id:string|null;body:string;created_at:string}
type Review={id:string;user_id:string;comic_id:string;body:string;created_at:string;updated_at:string}
type HistoryRow={user_id:string;comic_id:string;episode_id:string;last_read_at:string}
const demo:Comic[]=[
{id:'demo-1',title:'Neon Hearts',slug:'neon-hearts',description:'A runaway android and a street artist uncover a conspiracy hidden beneath the city.',cover_path:null,average_rating:4.8,rating_count:184,followers:18200,views:92000,status:'ongoing',content_status:'published',is_featured:true,created_by:'',chapters_count:24,last_episode_at:null},
{id:'demo-2',title:'Kingdom of Ash',slug:'kingdom-of-ash',description:'An exiled heir returns to a kingdom where magic has become a weapon.',cover_path:null,average_rating:4.7,rating_count:139,followers:14300,views:81000,status:'ongoing',content_status:'published',is_featured:false,created_by:'',chapters_count:18,last_episode_at:null},
{id:'demo-3',title:'Midnight Classroom',slug:'midnight-classroom',description:'Every night at midnight, one classroom opens to students from another world.',cover_path:null,average_rating:4.6,rating_count:91,followers:9700,views:54000,status:'ongoing',content_status:'published',is_featured:false,created_by:'',chapters_count:14,last_episode_at:null},
{id:'demo-4',title:'After the Rain',slug:'after-the-rain',description:'Two strangers rebuild their lives in a coastal town where everyone has a secret.',cover_path:null,average_rating:4.5,rating_count:72,followers:7600,views:41000,status:'completed',content_status:'published',is_featured:false,created_by:'',chapters_count:32,last_episode_at:null}
]
const asset=(bucket:string,path:string|null)=>path?supabase.storage.from(bucket).getPublicUrl(path).data.publicUrl:null
const slugify=(s:string)=>s.toLowerCase().trim().replace(/[^a-z0-9]+/g,'-').replace(/(^-|-$)/g,'').slice(0,80)
const imageTypes=['image/jpeg','image/png','image/webp','image/avif'];const validImage=(f:File,max:number)=>imageTypes.includes(f.type)&&f.size>0&&f.size<=max
const fields='id,title,slug,description,cover_path,average_rating,rating_count,followers,views,status,content_status,is_featured,created_by,chapters_count,last_episode_at'

export default function App(){
 const[session,setSession]=useState<Session|null>(null),[profile,setProfile]=useState<Profile|null>(null),[comics,setComics]=useState<Comic[]>([]),[selected,setSelected]=useState<Comic|null>(null),[episodes,setEpisodes]=useState<Episode[]>([]),[pages,setPages]=useState<Page[]>([]),[comments,setComments]=useState<Comment[]>([]),[reviews,setReviews]=useState<Review[]>([]),[history,setHistory]=useState<HistoryRow[]>([]),[query,setQuery]=useState(''),[view,setView]=useState<View>('home'),[episode,setEpisode]=useState<Episode|null>(null),[message,setMessage]=useState(''),[busy,setBusy]=useState(false),[bookmarked,setBookmarked]=useState(false),[followed,setFollowed]=useState(false),[myRating,setMyRating]=useState<number|null>(null),[reviewBody,setReviewBody]=useState(''),[commentBody,setCommentBody]=useState(''),[liked,setLiked]=useState<Set<string>>(new Set()),[navOpen,setNavOpen]=useState(false)
 const notify=(m:string)=>{setMessage(m);window.setTimeout(()=>setMessage(''),5000)}
 const refreshComics=async()=>{const{data,error}=await supabase.from('comics').select(fields).eq('content_status','published').order('ranking_score',{ascending:false}).limit(48);if(error){notify(error.message);return}setComics(data?.length?(data as Comic[]):demo)}
 const loadProfile=async(id:string)=>{const{data,error}=await supabase.from('profiles').select('id,username,display_name,avatar_url,bio,role,creator_approved,is_banned,banned_until').eq('id',id).maybeSingle();if(error)notify(error.message);setProfile((data as Profile)||null)}
 const loadHistory=async(id:string)=>{const{data}=await supabase.from('history').select('user_id,comic_id,episode_id,last_read_at').eq('user_id',id).order('last_read_at',{ascending:false}).limit(50);setHistory((data||[]) as HistoryRow[])}
 useEffect(()=>{supabase.auth.getSession().then(({data})=>{setSession(data.session);if(data.session){void loadProfile(data.session.user.id);void loadHistory(data.session.user.id)}});const{data:{subscription}}=supabase.auth.onAuthStateChange((_e,s)=>{setSession(s);if(s){void loadProfile(s.user.id);void loadHistory(s.user.id)}else{setProfile(null);setHistory([])}});void refreshComics();return()=>subscription.unsubscribe()},[])
 async function openComic(c:Comic){setSelected(c);setView('detail');setNavOpen(false);const[{data:eps,error:e1},{data:cms,error:e2},{data:rvs,error:e3}]=await Promise.all([supabase.from('episodes').select('id,comic_id,episode_number,title,description,content_status,published_at,views,created_by').eq('comic_id',c.id).eq('content_status','published').order('episode_number',{ascending:true}),supabase.from('comments').select('id,user_id,comic_id,episode_id,parent_id,body,created_at').eq('comic_id',c.id).order('created_at',{ascending:false}).limit(100),supabase.from('reviews').select('id,user_id,comic_id,body,created_at,updated_at').eq('comic_id',c.id).order('created_at',{ascending:false}).limit(50)]);if(e1||e2||e3)notify((e1||e2||e3)?.message||'Unable to load comic');setEpisodes((eps||[]) as Episode[]);setComments((cms||[]) as Comment[]);setReviews((rvs||[]) as Review[]);if(session){const[{data:b},{data:f},{data:r},{data:l}]=await Promise.all([supabase.from('bookmarks').select('comic_id').eq('user_id',session.user.id).eq('comic_id',c.id).maybeSingle(),supabase.from('comic_follows').select('comic_id').eq('user_id',session.user.id).eq('comic_id',c.id).maybeSingle(),supabase.from('ratings').select('rating').eq('user_id',session.user.id).eq('comic_id',c.id).maybeSingle(),supabase.from('comment_likes').select('comment_id').eq('user_id',session.user.id)]);setBookmarked(Boolean(b));setFollowed(Boolean(f));setMyRating(r?.rating||null);setLiked(new Set((l||[]).map(x=>x.comment_id)))}}
 async function signIn(){setBusy(true);const{error}=await supabase.auth.signInWithOAuth({provider:'github',options:{redirectTo:window.location.origin}});if(error)notify(error.message);setBusy(false)}
 async function signOut(){await supabase.auth.signOut();setView('home')}
 async function requireAuth(fn:()=>Promise<void>){if(!session){await signIn();return}await fn()}
 async function toggleBookmark(){if(!selected)return;await requireAuth(async()=>{const res=bookmarked?await supabase.from('bookmarks').delete().eq('user_id',session!.user.id).eq('comic_id',selected.id):await supabase.from('bookmarks').insert({user_id:session!.user.id,comic_id:selected.id});if(res.error)notify(res.error.message);else{setBookmarked(!bookmarked);notify(bookmarked?'Bookmark removed.':'Bookmark saved.')}})}
 async function toggleFollow(){if(!selected)return;await requireAuth(async()=>{const res=followed?await supabase.from('comic_follows').delete().eq('user_id',session!.user.id).eq('comic_id',selected.id):await supabase.from('comic_follows').insert({user_id:session!.user.id,comic_id:selected.id});if(res.error)notify(res.error.message);else{setFollowed(!followed);notify(followed?'Unfollowed.':'Following.')}})}
 async function rate(n:number){if(!selected)return;await requireAuth(async()=>{const res=await supabase.from('ratings').upsert({user_id:session!.user.id,comic_id:selected.id,rating:n});if(res.error)notify(res.error.message);else{setMyRating(n);notify('Rating saved.')}})}
 async function submitReview(){if(!selected||!reviewBody.trim())return;await requireAuth(async()=>{const{data:existing}=await supabase.from('reviews').select('id').eq('user_id',session!.user.id).eq('comic_id',selected.id).maybeSingle();const res=existing?await supabase.from('reviews').update({body:reviewBody.trim()}).eq('id',existing.id):await supabase.from('reviews').insert({user_id:session!.user.id,comic_id:selected.id,body:reviewBody.trim()});if(res.error)notify(res.error.message);else{setReviewBody('');notify('Review saved.');await openComic(selected)}})}
 async function submitComment(){if(!selected||!commentBody.trim())return;await requireAuth(async()=>{const res=await supabase.from('comments').insert({user_id:session!.user.id,comic_id:selected.id,episode_id:episode?.id||null,parent_id:null,body:commentBody.trim()});if(res.error)notify(res.error.message);else{setCommentBody('');notify('Comment posted.');await openComic(selected)}})}
 async function likeComment(id:string){if(!session){await signIn();return}const has=liked.has(id);const res=has?await supabase.from('comment_likes').delete().eq('user_id',session.user.id).eq('comment_id',id):await supabase.from('comment_likes').insert({user_id:session.user.id,comment_id:id});if(res.error)notify(res.error.message);else setLiked(prev=>{const n=new Set(prev);has?n.delete(id):n.add(id);return n})}
 async function report(){if(!selected)return;await requireAuth(async()=>{const reason=window.prompt('Reason for report')?.trim();if(!reason)return;const res=await supabase.from('reports').insert({reporter_id:session!.user.id,comic_id:selected.id,reason,status:'pending'});if(res.error)notify(res.error.message);else notify('Report submitted.')})}
 async function openEpisode(e:Episode){setEpisode(e);setView('reader');const{data,error}=await supabase.from('episode_pages').select('id,episode_id,page_number,storage_path,alt_text').eq('episode_id',e.id).order('page_number',{ascending:true});if(error){notify(error.message);return}setPages((data||[]) as Page[]);if(session){await Promise.all([supabase.from('reading_progress').upsert({user_id:session.user.id,comic_id:e.comic_id,episode_id:e.id,page_number:1,scroll_percent:0}),supabase.from('history').upsert({user_id:session.user.id,comic_id:e.comic_id,episode_id:e.id,last_read_at:new Date().toISOString()})]);void loadHistory(session.user.id)}await supabase.from('comic_views').insert({comic_id:e.comic_id,viewer_id:session?.user.id||null})}
 async function saveProgress(page:number,pct:number){if(!session||!episode)return;const p=Math.max(0,Math.min(100,pct));await Promise.all([supabase.from('reading_progress').upsert({user_id:session.user.id,comic_id:episode.comic_id,episode_id:episode.id,page_number:Math.max(1,page),scroll_percent:Number(p.toFixed(2))}),supabase.from('history').upsert({user_id:session.user.id,comic_id:episode.comic_id,episode_id:episode.id,last_read_at:new Date().toISOString()})])}
 const list=useMemo(()=>comics.filter(c=>(c.title+' '+c.description).toLowerCase().includes(query.toLowerCase())),[comics,query])
 const historyComics=useMemo(()=>history.map(h=>comics.find(c=>c.id===h.comic_id)).filter(Boolean) as Comic[],[history,comics])
 if(view==='reader'&&episode)return <Reader episode={episode} pages={pages} title={selected?.title||'Comic'} onBack={()=>{setView('detail');if(selected)void openComic(selected)}} onHome={()=>setView('home')} onProgress={saveProgress}/>
 if(view==='studio'&&session)return <Studio session={session} profile={profile} notify={notify} refresh={refreshComics} onBack={()=>setView('home')}/>
 if(view==='admin'&&session&&profile?.role==='admin')return <Admin notify={notify} onBack={()=>setView('home')}/>
 return <div className="app"><header className="topbar"><button className="brandBtn" onClick={()=>setView('home')}><span className="mark">W</span><div><strong>WebComicWorld</strong><small>Read beyond the panel</small></div></button><button className="mobileMenu" onClick={()=>setNavOpen(!navOpen)} aria-label="Menu">☰</button><nav className={navOpen?'open':''}><button onClick={()=>setView('home')}>Home</button><button onClick={()=>setView('browse')}>Browse</button><button onClick={()=>setView('browse')}>Rankings</button><button onClick={()=>setView('browse')}>Genres</button>{session&&<button onClick={()=>setView('library')}>Library</button>}{profile?.role==='admin'&&<button onClick={()=>setView('admin')}>Admin</button>}{profile?.creator_approved&&profile.role!=='reader'&&<button onClick={()=>setView('studio')}>Creator Studio</button>}</nav><div className="actions"><input value={query} onChange={e=>{setQuery(e.target.value);setView('browse')}} placeholder="Search comics…"/>{session?<><button onClick={()=>setView('profile')}>{profile?.display_name||'Profile'}</button><button onClick={signOut}>Sign out</button></>:<button onClick={signIn} disabled={busy}>{busy?'Connecting…':'Sign in with GitHub'}</button>}</div></header>{message&&<div className="notice" role="status">{message}</div>}<main>{view==='profile'&&profile?<ProfilePage profile={profile} history={historyComics} onOpen={openComic} onBack={()=>setView('home')}/>:view==='library'&&session?<Library session={session} comics={comics} onOpen={openComic} onBack={()=>setView('home')}/>:view==='detail'&&selected?<Detail comic={selected} episodes={episodes} comments={comments} reviews={reviews} liked={liked} bookmarked={bookmarked} followed={followed} rating={myRating} review={reviewBody} comment={commentBody} onReview={setReviewBody} onComment={setCommentBody} onBack={()=>setView('browse')} onEpisode={openEpisode} onBookmark={toggleBookmark} onFollow={toggleFollow} onRate={rate} onReviewSubmit={submitReview} onCommentSubmit={submitComment} onLike={likeComment} onReport={report}/>:<><section className="hero"><div><p className="eyebrow">A new home for webcomics</p><h1>Stories that keep<br/><span>you scrolling.</span></h1><p className="lead">Discover original comics, follow creators and read every episode in a beautiful, distraction-free reader.</p><div className="heroBtns"><button className="primary" onClick={()=>setView('browse')}>Explore comics</button><button className="secondary" onClick={()=>session?(profile?.creator_approved&&profile.role!=='reader'?setView('studio'):setView('profile')):signIn()}>{session?'Open account':'Become a creator'}</button></div></div><div className="heroCard"><div className="fakeCover">✦<br/><b>WEB<br/>COMIC<br/>WORLD</b></div></div></section><section className="section"><div className="sectionHead"><div><p className="eyebrow">{view==='browse'?'Search results':'Curated for you'}</p><h2>{view==='browse'?'Browse comics':'Trending now'}</h2></div></div><div className="grid">{list.map(c=><ComicCard key={c.id} comic={c} onClick={()=>openComic(c)}/>)}</div>{!list.length&&<div className="empty">No comics match your search.</div>}</section><section className="creator"><div><p className="eyebrow">For creators</p><h2>Build your audience.<br/>Own your story.</h2><p>Upload covers and episode pages directly to Supabase Storage, publish on your schedule, and see how readers engage.</p><button className="primary" onClick={()=>session?(profile?.creator_approved&&profile.role!=='reader'?setView('studio'):setView('profile')):signIn()}>Open Creator Studio →</button></div><div className="studio"><div className="studioTop"><span>CREATOR STUDIO</span><span>LIVE</span></div><div className="bars"><i/><i/><i/><i/><i/><i/><i/></div></div></section></>}</main><footer><span>© 2026 WebComicWorld</span><span>Discover · Read · Create</span></footer></div>
}
function ComicCard({comic,onClick}:{comic:Comic;onClick:()=>void}){return <article className="comic" onClick={onClick}><div className="cover">{asset('comic-covers',comic.cover_path)?<img src={asset('comic-covers',comic.cover_path)!} alt={comic.title}/>:<div>{comic.title.split(' ').slice(0,2).map(x=>x[0]).join('')}</div>}<span>{comic.status}</span></div><div className="comicInfo"><h3>{comic.title}</h3><p>{comic.description}</p><div className="meta"><span>★ {Number(comic.average_rating||0).toFixed(1)}</span><span>♡ {(comic.followers||0).toLocaleString()}</span><span>◉ {(comic.views||0).toLocaleString()}</span></div></div></article>}
function ProfilePage({profile,history,onOpen,onBack}:{profile:Profile;history:Comic[];onOpen:(c:Comic)=>void;onBack:()=>void}){return <section className="section pagePad"><div className="sectionHead"><div><p className="eyebrow">Account</p><h1>{profile.display_name||profile.username}</h1><p className="lead">@{profile.username} · {profile.role}</p></div><button className="secondary" onClick={onBack}>Back</button></div><div className="profileCard"><p>{profile.bio||'Welcome to WebComicWorld.'}</p><p>{profile.creator_approved?'Creator approved':'Reader account'}{profile.is_banned?' · Account restricted':''}</p></div><h2>Recent reading</h2><div className="grid">{history.map(c=><ComicCard key={c.id} comic={c} onClick={()=>onOpen(c)}/>)}</div>{!history.length&&<div className="empty">No reading history yet.</div>}</section>}
function Library({session,comics,onOpen,onBack}:{session:Session;comics:Comic[];onOpen:(c:Comic)=>void;onBack:()=>void}){const[tab,setTab]=useState<'bookmarks'|'follows'|'history'>('bookmarks'),[ids,setIds]=useState<string[]>([]),[loading,setLoading]=useState(false);useEffect(()=>{(async()=>{setLoading(true);const table=tab==='bookmarks'?'bookmarks':tab==='follows'?'comic_follows':'history';const order=tab==='history'?'last_read_at':'created_at';const{data,error}=await supabase.from(table).select('comic_id').eq('user_id',session.user.id).order(order,{ascending:false}).limit(100);if(error)console.error(error);setIds((data||[]).map(r=>r.comic_id));setLoading(false)})()},[tab,session.user.id]);const items=comics.filter(c=>ids.includes(c.id));return <section className="section pagePad"><div className="sectionHead"><div><p className="eyebrow">Your library</p><h1>Library</h1></div><button className="secondary" onClick={onBack}>Back</button></div><div className="tabs">{(['bookmarks','follows','history'] as const).map(t=><button key={t} className={tab===t?'primary':'secondary'} onClick={()=>setTab(t)}>{t[0].toUpperCase()+t.slice(1)}</button>)}</div>{loading?<div className="empty">Loading…</div>:<><div className="grid">{items.map(c=><ComicCard key={c.id} comic={c} onClick={()=>onOpen(c)}/>)}</div>{!items.length&&<div className="empty">Your {tab} is empty.</div>}</>}</section>}
function Detail({comic,episodes,comments,reviews,liked,bookmarked,followed,rating,review,comment,onReview,onComment,onBack,onEpisode,onBookmark,onFollow,onRate,onReviewSubmit,onCommentSubmit,onLike,onReport}:{comic:Comic;episodes:Episode[];comments:Comment[];reviews:Review[];liked:Set<string>;bookmarked:boolean;followed:boolean;rating:number|null;review:string;comment:string;onReview:(v:string)=>void;onComment:(v:string)=>void;onBack:()=>void;onEpisode:(e:Episode)=>void;onBookmark:()=>void;onFollow:()=>void;onRate:(n:number)=>void;onReviewSubmit:()=>void;onCommentSubmit:()=>void;onLike:(id:string)=>void;onReport:()=>void}){return <section className="detail"><button className="secondary" onClick={onBack}>← Browse</button><div className="detailGrid"><div className="detailCover">{asset('comic-covers',comic.cover_path)?<img src={asset('comic-covers',comic.cover_path)!} alt={comic.title}/>:<b>{comic.title}</b>}</div><div><p className="eyebrow">{comic.status}</p><h1>{comic.title}</h1><p className="lead">{comic.description}</p><div className="meta big"><span>★ {Number(comic.average_rating||0).toFixed(1)} ({comic.rating_count||0})</span><span>♡ {(comic.followers||0).toLocaleString()} followers</span><span>◉ {(comic.views||0).toLocaleString()} views</span></div><div className="buttonRow"><button className={bookmarked?'primary':'secondary'} onClick={onBookmark}>{bookmarked?'★ Bookmarked':'☆ Bookmark'}</button><button className={followed?'primary':'secondary'} onClick={onFollow}>{followed?'✓ Following':'Follow'}</button><button className="secondary" onClick={onReport}>Report</button>{episodes[0]&&<button className="primary" onClick={()=>onEpisode(episodes[0])}>Start reading</button>}</div><div className="rating"><strong>Your rating:</strong>{[1,2,3,4,5].map(n=><button key={n} className={rating===n?'star selected':'star'} onClick={()=>onRate(n)}>★</button>)}</div></div></div><h2>Episodes</h2><div className="episodes">{episodes.length?episodes.map(e=><button key={e.id} onClick={()=>onEpisode(e)}><b>Episode {e.episode_number}</b><span>{e.title}</span><small>{e.views.toLocaleString()} views</small></button>):<div className="empty">No published episodes yet.</div>}</div><div className="communityGrid"><section><h2>Reviews</h2><div className="composer"><textarea maxLength={5000} placeholder="Share your thoughts…" value={review} onChange={e=>onReview(e.target.value)}/><button className="primary" onClick={onReviewSubmit}>Save review</button></div>{reviews.map(r=><article className="communityItem" key={r.id}><strong>{r.user_id.slice(0,8)}</strong><small>{new Date(r.created_at).toLocaleDateString()}</small><p>{r.body}</p></article>)}</section><section><h2>Comments</h2><div className="composer"><textarea maxLength={3000} placeholder="Join the conversation…" value={comment} onChange={e=>onComment(e.target.value)}/><button className="primary" onClick={onCommentSubmit}>Post comment</button></div>{comments.map(c=><article className="communityItem" key={c.id}><strong>{c.user_id.slice(0,8)}</strong><small>{new Date(c.created_at).toLocaleDateString()}</small><p>{c.body}</p><button className="secondary small" onClick={()=>onLike(c.id)}>{liked.has(c.id)?'♥ Liked':'♡ Like'}</button></article>)}</section></div></section>}
function Reader({episode,pages,title,onBack,onHome,onProgress}:{episode:Episode;pages:Page[];title:string;onBack:()=>void;onHome:()=>void;onProgress:(p:number,s:number)=>void}){const[current,setCurrent]=useState(1);useEffect(()=>{const onScroll=()=>{if(!pages.length)return;const y=window.scrollY+window.innerHeight*0.45;let idx=1;pages.forEach(p=>{const el=document.querySelector(`[data-page="${p.page_number}"]`) as HTMLElement|null;if(el&&el.offsetTop<=y)idx=p.page_number});if(idx!==current){setCurrent(idx);void onProgress(idx,(idx/pages.length)*100)}};window.addEventListener('scroll',onScroll,{passive:true});return()=>window.removeEventListener('scroll',onScroll)},[pages.length,current,onProgress]);const go=(n:number)=>{const el=document.querySelector(`[data-page="${n}"]`);el?.scrollIntoView({behavior:'smooth'});setCurrent(n);void onProgress(n,(n/pages.length)*100)};return <div className="app reader"><header className="topbar"><button className="secondary" onClick={onBack}>← Back</button><strong>{title} · Episode {episode.episode_number}</strong><button className="secondary" onClick={onHome}>Home</button></header><main className="readerMain">{pages.length?pages.map(p=><img key={p.id} data-page={p.page_number} loading={p.page_number>2?'lazy':'eager'} src={asset('comic-pages',p.storage_path)!} alt={p.alt_text||`Page ${p.page_number}`} onClick={()=>go(p.page_number)}/>):<div className="empty">No pages have been published for this episode yet.</div>}</main><div className="readerFooter"><span>Page {pages.length?current:0}/{pages.length}</span><div><button className="secondary" disabled={current<=1} onClick={()=>go(current-1)}>Previous</button><button className="primary" disabled={current>=pages.length} onClick={()=>go(current+1)}>Next</button></div></div></div>}
function Studio({session,profile,notify,refresh,onBack}:{session:Session;profile:Profile|null;notify:(m:string)=>void;refresh:()=>Promise<void>;onBack:()=>void}){const[comics,setComics]=useState<Comic[]>([]),[eps,setEps]=useState<Episode[]>([]),[title,setTitle]=useState(''),[description,setDescription]=useState(''),[cover,setCover]=useState<File|null>(null),[comicId,setComicId]=useState(''),[episodeId,setEpisodeId]=useState(''),[episodeTitle,setEpisodeTitle]=useState(''),[episodeNo,setEpisodeNo]=useState('1'),[pages,setPages]=useState<FileList|null>(null),[busy,setBusy]=useState(false),[editId,setEditId]=useState('')
 const load=async()=>{const{data,error}=await supabase.from('comics').select(fields).eq('created_by',session.user.id).order('updated_at',{ascending:false});if(error)notify(error.message);setComics((data||[]) as Comic[])};const loadEpisodes=async(id:string)=>{setComicId(id);const{data,error}=await supabase.from('episodes').select('id,comic_id,episode_number,title,description,content_status,published_at,views,created_by').eq('comic_id',id).eq('created_by',session.user.id).order('episode_number',{ascending:true});if(error)notify(error.message);setEps((data||[]) as Episode[])};useEffect(()=>{void load()},[])
 if(!profile||profile.role==='reader'||!profile.creator_approved)return <main className="studioPage"><h1>Creator access</h1><p>Your account must be approved as a creator before publishing content.</p><button className="secondary" onClick={onBack}>Back</button></main>
 async function createComic(){if(!title.trim()){notify('Title is required.');return}setBusy(true);const{data,error}=await supabase.from('comics').insert({title:title.trim().slice(0,160),slug:slugify(title),description:description.trim().slice(0,10000),created_by:session.user.id,content_status:'draft',status:'draft',alternative_titles:[],language_code:'en',is_mature:false,is_featured:false}).select('id').single();if(error){notify(error.message);setBusy(false);return}if(cover){if(!validImage(cover,10*1024*1024)){notify('Cover must be JPEG, PNG, WebP or AVIF and no larger than 10 MB.');setBusy(false);return}const path=`${session.user.id}/${data.id}/${Date.now()}-${cover.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`;const up=await supabase.storage.from('comic-covers').upload(path,cover,{upsert:false,contentType:cover.type});if(up.error){await supabase.from('comics').delete().eq('id',data.id);notify(up.error.message);setBusy(false);return}const upd=await supabase.from('comics').update({cover_path:path}).eq('id',data.id);if(upd.error){await supabase.storage.from('comic-covers').remove([path]);await supabase.from('comics').delete().eq('id',data.id);notify(upd.error.message);setBusy(false);return}}setComicId(data.id);setTitle('');setDescription('');setCover(null);await load();await refresh();notify('Draft comic created.');setBusy(false)}
 async function saveEdit(){if(!editId||!title.trim())return;const{error}=await supabase.from('comics').update({title:title.trim().slice(0,160),slug:slugify(title),description:description.trim().slice(0,10000)}).eq('id',editId).eq('created_by',session.user.id);if(error)notify(error.message);else{notify('Comic updated.');setEditId('');setTitle('');setDescription('');await load()}}
 async function createEpisode(){const n=Number(episodeNo);if(!comicId||!Number.isInteger(n)||n<1||!episodeTitle.trim()){notify('Select a comic and enter a valid episode.');return}setBusy(true);const{data,error}=await supabase.from('episodes').insert({comic_id:comicId,episode_number:n,title:episodeTitle.trim().slice(0,200),slug:slugify(`${n}-${episodeTitle}`),description:'',content_status:'draft',created_by:session.user.id}).select('id').single();if(error)notify(error.message);else{setEpisodeId(data.id);setEpisodeTitle('');setEpisodeNo(String(n+1));await loadEpisodes(comicId);notify('Episode draft created.')}setBusy(false)}
 async function uploadPages(){if(!comicId||!episodeId||!pages?.length){notify('Select an episode and choose pages.');return}const files=Array.from(pages).sort((a,b)=>a.name.localeCompare(b.name,undefined,{numeric:true}));if(files.length>250||files.some(f=>!validImage(f,50*1024*1024))){notify('Pages must be JPEG, PNG, WebP or AVIF, max 50 MB each, max 250 pages.');return}setBusy(true);const uploaded:string[]=[];try{for(let i=0;i<files.length;i++){const f=files[i],path=`${session.user.id}/${comicId}/${episodeId}/${String(i+1).padStart(4,'0')}-${f.name.replace(/[^a-zA-Z0-9._-]/g,'-')}`;const up=await supabase.storage.from('comic-pages').upload(path,f,{upsert:false,contentType:f.type});if(up.error)throw new Error(up.error.message);uploaded.push(path);const ins=await supabase.from('episode_pages').insert({episode_id:episodeId,page_number:i+1,storage_path:path,alt_text:`Episode page ${i+1}`});if(ins.error)throw new Error(ins.error.message)}notify(`${files.length} pages uploaded as draft.`)}catch(e){if(uploaded.length)await supabase.storage.from('comic-pages').remove(uploaded);await supabase.from('episode_pages').delete().eq('episode_id',episodeId);notify(e instanceof Error?e.message:'Upload failed.')}finally{setBusy(false)}}
 const call=async(fn:string,id:string)=>{const{error}=await supabase.rpc(fn,fn.includes('episode')?{p_episode_id:id}:{p_comic_id:id});if(error)notify(error.message);else{notify(fn.startsWith('publish')?'Published.':'Unpublished.');await load();if(comicId)await loadEpisodes(comicId);await refresh()}}
 return <main className="studioPage"><div className="sectionHead"><div><p className="eyebrow">Creator Studio</p><h1>Your publishing workspace</h1><p className="lead">Draft, upload, review and publish safely.</p></div><button className="secondary" onClick={onBack}>Back</button></div><div className="formGrid"><section><h2>1. Create or edit comic</h2>{editId&&<p>Editing draft: {editId.slice(0,8)}</p>}<input maxLength={160} placeholder="Comic title" value={title} onChange={e=>setTitle(e.target.value)}/><textarea maxLength={10000} placeholder="Description" value={description} onChange={e=>setDescription(e.target.value)}/>{!editId&&<label>Cover image<input type="file" accept={imageTypes.join(',')} onChange={e=>setCover(e.target.files?.[0]||null)}/></label>}<button className="primary" disabled={busy||!title.trim()} onClick={editId?saveEdit:createComic}>{editId?'Save changes':'Create draft'}</button></section><section><h2>2. Create episode</h2><select value={comicId} onChange={e=>void loadEpisodes(e.target.value)}><option value="">Select your comic</option>{comics.map(c=><option key={c.id} value={c.id}>{c.title} · {c.content_status}</option>)}</select><input type="number" min="1" value={episodeNo} onChange={e=>setEpisodeNo(e.target.value)}/><input placeholder="Episode title" value={episodeTitle} onChange={e=>setEpisodeTitle(e.target.value)}/><button className="primary" disabled={busy||!comicId} onClick={createEpisode}>Create episode draft</button></section><section><h2>3. Upload pages</h2><select value={episodeId} onChange={e=>setEpisodeId(e.target.value)}><option value="">Select episode</option>{eps.map(e=><option key={e.id} value={e.id}>Episode {e.episode_number} · {e.content_status}</option>)}</select><label>Pages<input type="file" multiple accept={imageTypes.join(',')} onChange={e=>setPages(e.target.files)}/></label><button className="primary" disabled={busy} onClick={uploadPages}>Upload pages</button></section></div><section><h2>Your comics</h2>{comics.map(c=><article className="manageRow" key={c.id}><div><strong>{c.title}</strong><span>{c.content_status} · {c.chapters_count} published episodes · {c.views.toLocaleString()} views · {c.followers.toLocaleString()} followers</span></div><div><button className="secondary small" onClick={()=>{setEditId(c.id);setTitle(c.title);setDescription(c.description);void loadEpisodes(c.id)}}>Edit</button>{c.content_status==='published'?<button className="secondary small" onClick={()=>call('unpublish_comic',c.id)}>Unpublish</button>:<button className="primary small" onClick={()=>call('publish_comic',c.id)}>Publish</button>}</div></article>)}</section>{comicId&&<section><h2>Episodes</h2>{eps.map(e=><article className="manageRow" key={e.id}><div><strong>Episode {e.episode_number} · {e.title}</strong><span>{e.content_status} · {e.views.toLocaleString()} views</span></div><div>{e.content_status==='published'?<button className="secondary small" onClick={()=>call('unpublish_episode',e.id)}>Unpublish</button>:<button className="primary small" onClick={()=>call('publish_episode',e.id)}>Publish</button>}</div></article>)}</section>}</main>}
function Admin({notify,onBack}:{notify:(m:string)=>void;onBack:()=>void}){const[profiles,setProfiles]=useState<Profile[]>([]),[comics,setComics]=useState<Comic[]>([]),[reports,setReports]=useState<any[]>([]);const load=async()=>{const[{data:p},{data:c},{data:r}]=await Promise.all([supabase.from('profiles').select('id,username,display_name,avatar_url,bio,role,creator_approved,is_banned,banned_until').order('created_at',{ascending:false}).limit(200),supabase.from('comics').select(fields).order('created_at',{ascending:false}).limit(200),supabase.from('reports').select('*').order('created_at',{ascending:false}).limit(200)]);setProfiles((p||[]) as Profile[]);setComics((c||[]) as Comic[]);setReports(r||[])};useEffect(()=>{void load()},[]);const rpc=async(name:string,args:any,ok:string)=>{const{error}=await supabase.rpc(name,args);if(error)notify(error.message);else{notify(ok);await load()}};return <main className="section pagePad"><div className="sectionHead"><div><p className="eyebrow">Administration</p><h1>Moderation dashboard</h1></div><button className="secondary" onClick={onBack}>Back</button></div><section><h2>Users</h2>{profiles.map(p=><article className="manageRow" key={p.id}><div><strong>{p.display_name||p.username}</strong><span>@{p.username} · {p.role} · {p.creator_approved?'creator approved':'not approved'}{p.is_banned?' · banned':''}</span></div><div><button className="secondary small" onClick={()=>rpc('admin_set_creator_approval',{p_user_id:p.id,p_approved:!p.creator_approved},p.creator_approved?'Creator approval removed.':'Creator approved.')}>{p.creator_approved?'Remove creator':'Approve creator'}</button><button className="secondary small" onClick={()=>rpc('admin_set_banned',{p_user_id:p.id,p_banned:!p.is_banned},p.is_banned?'User unbanned.':'User banned.')}>{p.is_banned?'Unban':'Ban'}</button></div></article>)}</section><section><h2>Comics</h2>{comics.map(c=><article className="manageRow" key={c.id}><div><strong>{c.title}</strong><span>{c.content_status} · {c.views.toLocaleString()} views · {c.followers.toLocaleString()} followers</span></div><div><button className="secondary small" onClick={()=>rpc('admin_feature_comic',{p_comic_id:c.id,p_featured:!c.is_featured},c.is_featured?'Unfeatured.':'Featured.')}>{c.is_featured?'Unfeature':'Feature'}</button><button className="secondary small" onClick={()=>rpc('admin_set_comic_moderation',{p_comic_id:c.id,p_content_status:c.content_status==='published'?'archived':'published'},c.content_status==='published'?'Archived.':'Published.')}>{c.content_status==='published'?'Archive':'Publish'}</button></div></article>)}</section><section><h2>Reports ({reports.length})</h2>{reports.map(r=><article className="manageRow" key={r.id}><div><strong>{r.reason}</strong><span>{r.status} · {new Date(r.created_at).toLocaleString()}</span></div><div>{r.status==='pending'&&<button className="primary small" onClick={()=>rpc('noop',{},'')}>Review in database</button>}</div></article>)}</section></main>}
